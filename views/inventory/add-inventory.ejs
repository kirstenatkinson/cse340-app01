<% if (title) { %>
    <h1 id="add-inventory-page-title"><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

    <% if (notice && notice.length > 0) { %>
      <div class="notice-message">
        <%= notice %>
      </div>
    <% } %>
    <% if (error && error.length > 0) { %>
      <ul class="error">
        <% error.forEach(msg => { %>
          <li><%= msg %></li>
        <% }) %>
      </ul>
    <% } %>

    <form action="/inv/add-inventory" method="post" id="add-inventory-form">
        <fieldset id="new-inventory-information">
            <legend>Add a New Vehicle</legend>
            <label for="classificationList">Classification</label>
            <%- classificationList %> <!-- Render the dropdown list -->
            
            <label for="inv_make">Make</label>
            <input type="text" name="inv_make" id="inv_make" required value="<%= locals.inv_make || '' %>">
        
            <label for="inv_model">Model</label>
            <input type="text" name="inv_model" id="inv_model" required value="<%= locals.inv_model || '' %>">
        
            <label for="inv_year">Year</label>
            <input type="number" name="inv_year" id="inv_year" required value="<%= locals.inv_year || '' %>">
        
            <label for="inv_description">Description</label>
            <textarea name="inv_description" id="inv_description" required><%= locals.inv_description || '' %></textarea>
      
            <label for="inv_price">Price</label>
            <input type="number" name="inv_price" id="inv_price" required value="<%= locals.inv_price || '' %>">
        
            <label for="inv_miles">Miles</label>
            <input type="number" name="inv_miles" id="inv_miles" required value="<%= locals.inv_miles || '' %>">
        
            <label for="inv_color">Color</label>
            <input type="text" name="inv_color" id="inv_color" required value="<%= locals.inv_color || '' %>">

            <label for="inv_image">Image Path</label>
            <input type="text" name="inv_image" id="inv_image" required value="<%= locals.inv_image || '' %>">

            <label for="inv_thumbnail">Image Thumbnail</label>
            <input type="text" name="inv_thumbnail" id="inv_thumbnail" required value="<%= locals.inv_thumbnail || '' %>">

        </fieldset>
        <input type="submit" id="new-inventory-submit" value="Add New Inventory">
    </form>